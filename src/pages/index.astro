---
import Layout from '../layouts/Layout.astro';
import AdSense from '../components/AdSense.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('blog'))
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
  .slice(0, 3);
---

<Layout
  title="Ferramentas Gratuitas de Segurança Online"
  description="Analise a força de senhas, verifique URLs maliciosas e descubra se seu email vazou. Ferramentas gratuitas de cibersegurança para brasileiros."
>
  <!-- Hero Section -->
  <section class="relative overflow-hidden bg-gradient-to-br from-primary-900 via-primary-800 to-blue-900 text-white py-20">
    <div class="absolute inset-0 bg-grid-white/5 [mask-image:linear-gradient(0deg,transparent,rgba(255,255,255,0.6))]"></div>
    <!-- Animated blobs -->
    <div class="absolute top-20 left-10 w-64 h-64 bg-blue-500/20 rounded-full blur-3xl animate-pulse-slow"></div>
    <div class="absolute bottom-10 right-10 w-96 h-96 bg-indigo-500/20 rounded-full blur-3xl animate-pulse-slow" style="animation-delay: 1.5s"></div>

    <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <div class="inline-flex items-center gap-2 bg-white/10 backdrop-blur-sm border border-white/20 rounded-full px-4 py-2 text-sm mb-6">
        🇧🇷 Feito para brasileiros · 100% gratuito
      </div>
      <h1 class="text-4xl md:text-6xl font-bold mb-6 leading-tight">
        Sua Segurança Digital<br/>
        <span class="text-blue-300">Começa Aqui</span>
      </h1>
      <p class="text-xl text-blue-100 max-w-2xl mx-auto mb-10">
        Ferramentas profissionais de cibersegurança, gratuitas e sem cadastro. Proteja-se de senhas fracas, links maliciosos e vazamentos de dados.
      </p>
      <div class="flex flex-wrap gap-4 justify-center">
        <a href="/tools/senha" class="px-8 py-4 bg-white text-primary-900 font-bold rounded-xl hover:bg-blue-50 transition-all shadow-lg hover:shadow-xl hover:-translate-y-0.5">
          🔐 Analisar Senha
        </a>
        <a href="/tools/vazamento" class="px-8 py-4 bg-white/10 backdrop-blur border border-white/30 text-white font-bold rounded-xl hover:bg-white/20 transition-all">
          📧 Verificar Vazamentos
        </a>
      </div>
    </div>
  </section>

  <!-- Stats Bar -->
  <section class="bg-white dark:bg-gray-900 border-b border-gray-200 dark:border-gray-800 py-6">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-2 md:grid-cols-4 gap-6 text-center">
        {[
          { value: '15B+', label: 'Credenciais Vazadas no Mundo' },
          { value: '100%', label: 'Análise Local (Zero Upload)' },
          { value: '<50ms', label: 'Latência de Verificação' },
          { value: 'LGPD', label: 'Compliance Garantido' },
        ].map(({ value, label }) => (
          <div>
            <div class="text-2xl md:text-3xl font-bold text-primary-600 dark:text-primary-400">{value}</div>
            <div class="text-sm text-gray-500 dark:text-gray-400 mt-1">{label}</div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Tools Cards -->
  <section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
    <h2 class="text-3xl font-bold text-center mb-4">Ferramentas de Segurança</h2>
    <p class="text-gray-600 dark:text-gray-400 text-center max-w-xl mx-auto mb-10">
      Todas as ferramentas são gratuitas, sem cadastro e respeitam sua privacidade.
    </p>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
      {[
        {
          icon: '🔐',
          title: 'Analisador de Senhas',
          desc: 'Avalie a força da sua senha em tempo real com análise local. Score visual, tempo estimado de crack e gerador de senhas fortes.',
          href: '/tools/senha',
          tags: ['100% Local', 'OWASP', 'Grátis'],
          color: 'from-blue-500 to-indigo-600',
        },
        {
          icon: '🔗',
          title: 'Scanner de URLs',
          desc: 'Verifique se um link é malicioso antes de clicar. Powered by Google Safe Browsing com resposta em menos de 50ms.',
          href: '/tools/link',
          tags: ['Safe Browsing', '<50ms', 'Phishing'],
          color: 'from-purple-500 to-pink-600',
        },
        {
          icon: '📧',
          title: 'Verificador de Vazamentos',
          desc: 'Descubra se seu email apareceu em vazamentos de dados. Relatório completo com recomendações LGPD.',
          href: '/tools/vazamento',
          tags: ['XposedOrNot', 'LGPD', 'Breaches'],
          color: 'from-orange-500 to-red-600',
        },
      ].map(tool => (
        <a
          href={tool.href}
          class="group relative bg-white dark:bg-gray-800 rounded-2xl p-6 border border-gray-200 dark:border-gray-700 hover:border-primary-300 dark:hover:border-primary-700 shadow-sm hover:shadow-lg transition-all duration-300 hover:-translate-y-1"
        >
          <div class={`w-14 h-14 rounded-2xl bg-gradient-to-br ${tool.color} flex items-center justify-center text-2xl mb-4 shadow-lg`}>
            {tool.icon}
          </div>
          <h3 class="text-xl font-bold mb-2 group-hover:text-primary-600 dark:group-hover:text-primary-400 transition-colors">
            {tool.title}
          </h3>
          <p class="text-gray-600 dark:text-gray-400 text-sm mb-4">{tool.desc}</p>
          <div class="flex flex-wrap gap-2">
            {tool.tags.map(tag => (
              <span class="text-xs bg-gray-100 dark:bg-gray-700 text-gray-600 dark:text-gray-300 px-2 py-1 rounded-full">
                {tag}
              </span>
            ))}
          </div>
          <div class="absolute bottom-6 right-6 text-gray-400 group-hover:text-primary-500 transition-colors">
            →
          </div>
        </a>
      ))}
    </div>
  </section>

  <!-- AdSense Mid-page -->
  <AdSense slot="MID_PAGE_SLOT_ID" format="auto" className="my-4" />

  <!-- Blog Posts -->
  {posts.length > 0 && (
    <section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
      <div class="flex justify-between items-center mb-8">
        <h2 class="text-3xl font-bold">Blog de Segurança</h2>
        <a href="/blog" class="text-primary-600 dark:text-primary-400 hover:underline text-sm font-medium">
          Ver todos os posts →
        </a>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        {posts.map(post => (
          <a
            href={`/blog/${post.slug}`}
            class="group bg-white dark:bg-gray-800 rounded-2xl overflow-hidden border border-gray-200 dark:border-gray-700 hover:border-primary-300 dark:hover:border-primary-700 shadow-sm hover:shadow-lg transition-all duration-300 hover:-translate-y-0.5"
          >
            <div class="p-6">
              <div class="flex flex-wrap gap-2 mb-3">
                {post.data.tags.slice(0,2).map(tag => (
                  <span class="text-xs bg-primary-50 dark:bg-primary-950 text-primary-600 dark:text-primary-400 px-2 py-0.5 rounded-full">
                    #{tag}
                  </span>
                ))}
              </div>
              <h3 class="font-bold text-gray-900 dark:text-white mb-2 group-hover:text-primary-600 dark:group-hover:text-primary-400 transition-colors leading-snug">
                {post.data.title}
              </h3>
              <p class="text-gray-600 dark:text-gray-400 text-sm mb-4 line-clamp-2">
                {post.data.description}
              </p>
              <div class="flex justify-between items-center text-xs text-gray-400">
                <span>{post.data.author}</span>
                <time datetime={post.data.pubDate.toISOString()}>
                  {post.data.pubDate.toLocaleDateString('pt-BR', { day: '2-digit', month: 'short', year: 'numeric' })}
                </time>
              </div>
            </div>
          </a>
        ))}
      </div>
    </section>
  )}

  <!-- Trust Signals -->
  <section class="bg-gray-50 dark:bg-gray-900 border-t border-gray-200 dark:border-gray-800 py-12">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h2 class="text-2xl font-bold mb-8">Por que confiar no SegurançaOnline?</h2>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
        {[
          { icon: '🔒', title: 'Sem Coleta de Dados', desc: 'Senhas analisadas localmente. Nunca enviamos para servidores.' },
          { icon: '⚡', title: 'Ultra Rápido', desc: 'Infraestrutura Cloudflare. Latência <50ms em todo o Brasil.' },
          { icon: '📋', title: 'LGPD Compliant', desc: 'Conformidade total com a Lei Geral de Proteção de Dados.' },
          { icon: '🆓', title: 'Sempre Gratuito', desc: 'Ferramentas profissionais sem custo, sem cadastro.' },
        ].map(item => (
          <div class="bg-white dark:bg-gray-800 rounded-2xl p-6 border border-gray-200 dark:border-gray-700 text-center">
            <div class="text-4xl mb-3">{item.icon}</div>
            <h3 class="font-bold mb-1">{item.title}</h3>
            <p class="text-sm text-gray-600 dark:text-gray-400">{item.desc}</p>
          </div>
        ))}
      </div>
    </div>
  </section>
</Layout>
