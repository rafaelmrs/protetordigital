---
import Layout from '../../layouts/Layout.astro';
import AdSense from '../../components/AdSense.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('blog'))
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());
---

<Layout
  title="Blog de Segurança Digital — Dicas, Vazamentos e Proteção Online"
  description="Artigos educativos sobre cibersegurança, privacidade, LGPD e proteção de dados. Conteúdo gratuito para brasileiros se protegerem online."
  url="https://segurancaonline.com.br/blog"
>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <nav class="text-sm text-gray-500 dark:text-gray-400 mb-6" aria-label="breadcrumb">
      <ol class="flex items-center gap-2">
        <li><a href="/" class="hover:text-primary-600 dark:hover:text-primary-400">Home</a></li>
        <li>›</li>
        <li class="text-gray-800 dark:text-gray-200">Blog</li>
      </ol>
    </nav>

    <div class="mb-10">
      <h1 class="text-3xl md:text-4xl font-bold mb-3">📝 Blog de Segurança</h1>
      <p class="text-lg text-gray-600 dark:text-gray-400">
        Artigos semanais sobre cibersegurança, privacidade digital e proteção de dados no Brasil.
      </p>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
      <div class="lg:col-span-2">
        <div class="space-y-6">
          {posts.map((post, i) => (
            <>
              <article class="bg-white dark:bg-gray-800 rounded-2xl overflow-hidden border border-gray-200 dark:border-gray-700 hover:border-primary-300 dark:hover:border-primary-700 shadow-sm hover:shadow-lg transition-all duration-300">
                <a href={`/blog/${post.slug}`} class="block p-6">
                  <div class="flex flex-wrap gap-2 mb-3">
                    {post.data.tags.map(tag => (
                      <span class="text-xs bg-primary-50 dark:bg-primary-950 text-primary-600 dark:text-primary-400 px-2 py-0.5 rounded-full">
                        #{tag}
                      </span>
                    ))}
                    {post.data.featured && (
                      <span class="text-xs bg-amber-100 dark:bg-amber-950 text-amber-700 dark:text-amber-300 px-2 py-0.5 rounded-full">
                        ⭐ Destaque
                      </span>
                    )}
                  </div>
                  <h2 class="text-xl font-bold text-gray-900 dark:text-white mb-2 hover:text-primary-600 dark:hover:text-primary-400 transition-colors">
                    {post.data.title}
                  </h2>
                  <p class="text-gray-600 dark:text-gray-400 text-sm mb-4">
                    {post.data.description}
                  </p>
                  <div class="flex justify-between items-center text-xs text-gray-400">
                    <span class="flex items-center gap-1">✍️ {post.data.author}</span>
                    <time datetime={post.data.pubDate.toISOString()}>
                      {post.data.pubDate.toLocaleDateString('pt-BR', { day: '2-digit', month: 'long', year: 'numeric' })}
                    </time>
                  </div>
                </a>
              </article>

              {/* AdSense after 2nd post */}
              {i === 1 && (
                <AdSense slot="BETWEEN_RESULTS_SLOT_ID" format="auto" />
              )}
            </>
          ))}
        </div>

        {posts.length === 0 && (
          <div class="text-center py-16 text-gray-500">
            <p class="text-4xl mb-3">📝</p>
            <p class="text-lg font-medium">Posts em breve!</p>
            <p class="text-sm">Estamos preparando conteúdo de qualidade sobre segurança digital.</p>
          </div>
        )}
      </div>

      <aside class="space-y-6">
        <AdSense slot="SIDEBAR_SLOT_ID" width={300} height={600} />

        <!-- RSS link -->
        <div class="bg-orange-50 dark:bg-orange-950 rounded-2xl p-5 border border-orange-200 dark:border-orange-800">
          <h3 class="font-bold text-orange-800 dark:text-orange-200 mb-2">📡 RSS Feed</h3>
          <p class="text-sm text-orange-700 dark:text-orange-300 mb-3">Receba novos artigos no seu leitor de RSS.</p>
          <a href="/rss.xml" class="block text-center py-2 bg-orange-500 hover:bg-orange-600 text-white font-medium rounded-lg text-sm transition-colors">
            Assinar RSS
          </a>
        </div>

        <!-- Topics -->
        <div class="bg-white dark:bg-gray-800 rounded-2xl p-5 border border-gray-200 dark:border-gray-700">
          <h3 class="font-bold mb-3">🏷️ Tópicos</h3>
          <div class="flex flex-wrap gap-2">
            {[...new Set(posts.flatMap(p => p.data.tags))].map(tag => (
              <span class="text-xs bg-gray-100 dark:bg-gray-700 text-gray-600 dark:text-gray-300 px-3 py-1.5 rounded-full hover:bg-primary-100 dark:hover:bg-primary-900 cursor-pointer transition-colors">
                #{tag}
              </span>
            ))}
          </div>
        </div>

        <!-- Tools CTA -->
        <div class="bg-gradient-to-br from-primary-600 to-indigo-700 rounded-2xl p-5 text-white">
          <h3 class="font-bold mb-2">🛡️ Proteja-se Agora</h3>
          <p class="text-blue-100 text-sm mb-3">Use nossas ferramentas gratuitas para verificar sua segurança digital.</p>
          <div class="space-y-2">
            <a href="/tools/senha" class="block text-center py-2 bg-white/20 hover:bg-white/30 rounded-lg text-sm font-medium transition-colors">
              🔐 Analisar Senha
            </a>
            <a href="/tools/vazamento" class="block text-center py-2 bg-white/20 hover:bg-white/30 rounded-lg text-sm font-medium transition-colors">
              📧 Ver Vazamentos
            </a>
          </div>
        </div>
      </aside>
    </div>
  </div>
</Layout>
